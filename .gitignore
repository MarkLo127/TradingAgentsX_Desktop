# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 內建後端執行環境（由 scripts/build-backend.sh 產生，體積大不進 git）
resources/pybackend/
resources/appsrc/
release/

# 本機找不到 TradingAgentsX 時，build-backend.sh 下載的後端原始碼快取
.cache/

# 安裝檔一律走 GitHub Release，不進 git。
# GitHub 對版本庫內單一檔案的硬限制是 100 MB，而 DMG 約 370 MB —— push 會被直接拒絕。
# Release 資產的上限是 2 GB，才是散佈二進位檔的正確管道。
*.dmg
*.zip
*.blockmap
